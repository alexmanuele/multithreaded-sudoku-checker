Authors: I was the sole author.
compile string:
gcc -pthread -o A2 A2.c
run string:
./A2 Sud_Sol.txt

**Note: The program requires the input file as the command line argument. will return error if a different command line format is used.
